// import { PageData } from "vuepress";

// export const pages: Record<string, PageData> = {};

// type Pages = typeof pages;

// declare global {
//   var pages: Pages;
// }

// // export function getPages(app: App): PageData[] {
// //   return app.pages.map((page) => page.data);
// // }
